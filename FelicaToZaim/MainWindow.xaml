<Window x:Class="FelicaToZaim.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FelicaToZaim"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Button x:Name="button" Content="読み込み" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Width="75" Click="button_Click"/>
        <DataGrid x:Name="dataGrid" ItemsSource="{Binding}" AutoGenerateColumns="True" HorizontalAlignment="Left" Margin="10,39,0,0" VerticalAlignment="Top" Height="271" Width="497">
            <DataGrid.Columns>
                <!--DataGridCheckBoxColumn Header="Commit"/-->
                <!--DataGridTextColumn Header="端末"  Binding="{Binding Tanmatu}"/ -->
                <!--DataGridTextColumn Header="Proc" /-->
                <!--DataGridTextColumn Header="Date" /-->
                <!--DataGridTextColumn Header="Iri" /-->
                <!--DataGridTextColumn Header="Zandaka" /-->
                <!--DataGridTextColumn Header="Renban" /-->
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
